## Tracker

iOS‑приложение для учёта привычек/трекеров (MVVM + Core Data).

###Цели приложения:
- Контроль привычек по дням недели;
- Просмотр прогресса по привычкам;

###Описание приложения
- Приложение состоит из карточек-трекеров, которые создает пользователь. Он может указать название, категорию и задать расписание. Также можно выбрать эмодзи и цвет, чтобы отличать карточки друг от друга.
- Карточки отсортированы по категориям. Пользователь может искать их с помощью поиска и фильтровать.
- С помощью календаря пользователь может посмотреть какие привычки у него запланированы на конкретный день.
- В приложении есть статистика, которая отражает успешные показатели пользователя, его прогресс и средние значения.

###Темная тема🌚🌝
- В приложении есть темная тема, которая меняется в зависимости от настроек системы устройства.

###Локализация
- Приложение переведено на EN, DE, RU.

### Требования
- Xcode 16+ / iOS 17.6+
- Зависимости - только системные и AppMetrica (через SPM)

### Быстрый старт
1) Клонировать репозиторий  
2) Скопировать `Config/Secrets.example.xcconfig` ⏭️ `Config/Secrets.xcconfig` и прописать ключ AppMetrica (`APP_METRICA_API_KEY`)  
3) Открыть `Tracker.xcodeproj` в Xcode, собрать схему `Tracker`

### Тесты
- Снапшот‑тесты в `TrackerTests`; снапшоты лежат в `TrackerTests/__Snapshots__`
- Для перезаписи всех снапшотов использовать в любом тесте isRecording = true
- Для перезаписи определенного теста прописать в функцию дополнительный параметр -  assertSnapshot(matching: vc, as: .image, 👉👉👉record: true👈👈👈)

### Архитектура
- MVVM: ViewModel отвечает за стейт/фильтры, ViewController — за отображение и биндинг
- Core Data + NSFetchedResultsController в слое Stores (`Tracker/Core/Services/Store.swift`)
- Фичи разложены по папкам `Tracker/Features` (Trackers, Categories, Creation, Schedule, Stats)
- Ресурсы/локализация в `Tracker/Resources`

### Аналитика и конфиги
- AppMetrica активируется в `AppDelegate` через `AnalyticsService`
- Ключи и конфиги хранятся в `.xcconfig`

### Полезно знать
- Локализация: ru/en/de, строки через `NSLocalizedString`
- Пустые/поисковые состояния обрабатываются во `TrackersViewModel`

